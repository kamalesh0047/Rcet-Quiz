export interface PlayerSession { pin: string; playerId: string; token: string; nickname: string; }const key = (pin: string) => `quiz:session:${pin}`;export const session = {  save: (s: PlayerSession) => sessionStorage.setItem(key(s.pin), JSON.stringify(s)),  load: (pin: string): PlayerSession | null => { const v = sessionStorage.getItem(key(pin)); return v ? JSON.parse(v) : null; },  clear: (pin: string) => sessionStorage.removeItem(key(pin)),};
