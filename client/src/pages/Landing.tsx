import { Link } from 'react-router-dom';import { motion } from 'framer-motion';import { Zap, Users, BarChart3, ShieldCheck } from 'lucide-react';
export default function Landing() {  return (    <main className="min-h-full bg-[radial-gradient(ellipse_at_top,#4c1d95_0%,#020617_60%)]">      <nav className="mx-auto flex max-w-6xl items-center justify-between px-6 py-6">        <span className="font-display text-2xl font-extrabold">Quiz<span className="text-brand-500">Live</span></span>        <div className="flex gap-3"><Link to="/login" className="btn-ghost">Teacher login</Link><Link to="/join" className="btn-primary">Join a game</Link></div>      </nav>      <section className="mx-auto max-w-4xl px-6 pb-24 pt-16 text-center">        <motion.h1 initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} className="font-display text-5xl font-extrabold leading-tight md:text-7xl">          Live quizzes for <span className="bg-gradient-to-r from-fuchsia-400 to-violet-400 bg-clip-text text-transparent">500+ players</span> at once        </motion.h1>        <p className="mx-auto mt-6 max-w-2xl text-lg text-white/70">Students join with a PIN or QR code in seconds. You run the show, see every answer live, and export the results.</p>        <div className="mt-10 flex flex-wrap justify-center gap-4">          <Link to="/join" className="btn-primary px-8 py-4 text-lg">Enter game PIN</Link>          <Link to="/register" className="btn-ghost px-8 py-4 text-lg">Create a quiz</Link>        </div>        <div className="mt-20 grid gap-4 text-left sm:grid-cols-2 lg:grid-cols-4">          {[[Zap, 'Real-time', 'WebSocket broadcasts, no polling, sub-second updates.'],            [Users, 'Massive rooms', 'Built and load-tested for 500 to 10,000 concurrent players.'],            [BarChart3, 'Rich analytics', 'Accuracy, response time, hardest question, CSV/Excel/PDF exports.'],            [ShieldCheck, 'Server-authoritative', 'Timers, scoring and answers are validated on the server.']]            .map(([Icon, t, d]: any) => (              <div key={t} className="card"><Icon className="mb-3 h-7 w-7 text-brand-500" /><h3 className="font-display font-bold">{t}</h3><p className="mt-1 text-sm text-white/60">{d}</p></div>))}        </div>      </section>    </main>  );}
