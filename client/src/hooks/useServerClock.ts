import { useRef, useCallback } from 'react';/** Tracks offset between server and local clock so countdowns match the authoritative deadline. */export function useServerClock() {  const offset = useRef(0);  const sync = useCallback((serverNow: number) => { offset.current = serverNow - Date.now(); }, []);  const now = useCallback(() => Date.now() + offset.current, []);  return { sync, now };}
