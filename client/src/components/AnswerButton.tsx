import { motion } from 'framer-motion';import { Triangle, Diamond, Circle, Square } from 'lucide-react';import type { AnswerIndex } from '@shared/types';
const META = [  { bg: 'bg-ans-a', Icon: Triangle, label: 'A' }, { bg: 'bg-ans-b', Icon: Diamond, label: 'B' },  { bg: 'bg-ans-c', Icon: Circle, label: 'C' },   { bg: 'bg-ans-d', Icon: Square, label: 'D' },];
interface Props { index: AnswerIndex; text?: string; selected?: boolean; disabled?: boolean;  reveal?: 'correct' | 'wrong' | null; compact?: boolean; onSelect?: (i: AnswerIndex) => void; }
export function AnswerButton({ index, text, selected, disabled, reveal, compact, onSelect }: Props) {  const { bg, Icon, label } = META[index];  const dim = reveal === 'wrong' || (disabled && selected === false && reveal === null);  return (    <motion.button      type="button" disabled={disabled} onClick={() => onSelect?.(index)}      whileTap={disabled ? undefined : { scale: 0.97 }}      animate={{ opacity: dim ? 0.35 : 1, scale: selected ? 1.02 : 1 }}      className={`${bg} relative flex w-full items-center gap-3 rounded-2xl text-left text-white shadow-lg        ${compact ? 'min-h-[64px] px-4 py-3' : 'min-h-[88px] px-5 py-5 md:min-h-[120px]'}        ${selected ? 'ring-4 ring-white' : ''} ${reveal === 'correct' ? 'ring-4 ring-emerald-300' : ''}`}      aria-label={`Answer ${label}${text ? `: ${text}` : ''}`}    >      <Icon className="h-7 w-7 shrink-0 fill-current" />      <span className={`font-display font-bold ${compact ? 'text-base' : 'text-lg md:text-2xl'}`}>{text ?? label}</span>      {reveal === 'correct' && <span className="ml-auto rounded-full bg-white/25 px-3 py-1 text-xs font-bold">CORRECT</span>}    </motion.button>  );}
