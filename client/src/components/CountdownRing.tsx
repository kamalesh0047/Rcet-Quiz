export function CountdownRing({ secondsLeft, progress, size = 84 }: { secondsLeft: number; progress: number; size?: number }) {  const r = (size - 10) / 2, c = 2 * Math.PI * r;  const urgent = secondsLeft <= 5;  return (    <div className="relative grid place-items-center" style={{ width: size, height: size }}>      <svg width={size} height={size} className="-rotate-90">        <circle cx={size / 2} cy={size / 2} r={r} stroke="rgba(255,255,255,.15)" strokeWidth="8" fill="none" />        <circle cx={size / 2} cy={size / 2} r={r} stroke={urgent ? '#f43f5e' : '#a78bfa'} strokeWidth="8" fill="none"          strokeLinecap="round" strokeDasharray={c} strokeDashoffset={c * (1 - progress)} style={{ transition: 'stroke .3s' }} />      </svg>      <span className={`absolute font-display text-2xl font-extrabold tabular-nums ${urgent ? 'text-rose-400 animate-pulse' : ''}`}>        {Math.ceil(secondsLeft)}      </span>    </div>  );}
