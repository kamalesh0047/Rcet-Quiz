import { useEffect } from 'react';import confetti from 'canvas-confetti';export function Confetti({ burst = 3 }: { burst?: number }) {  useEffect(() => {    const reduced = matchMedia('(prefers-reduced-motion: reduce)').matches; if (reduced) return;    const timers = Array.from({ length: burst }, (_, i) => setTimeout(() =>      confetti({ particleCount: 90, spread: 80, startVelocity: 40, origin: { x: 0.2 + 0.3 * i, y: 0.6 }, ticks: 180 }), i * 400));    return () => timers.forEach(clearTimeout);  }, [burst]);  return null;}
