import { motion, AnimatePresence } from 'framer-motion';import { useEffect, useRef } from 'react';import gsap from 'gsap';import type { LeaderboardEntry } from '@shared/types';
const MEDAL = ['🥇', '🥈', '🥉'];
function Score({ value }: { value: number }) {  const ref = useRef<HTMLSpanElement>(null); const prev = useRef(0);  useEffect(() => {    const o = { v: prev.current };    gsap.to(o, { v: value, duration: 0.9, ease: 'power2.out', onUpdate: () => { if (ref.current) ref.current.textContent = Math.round(o.v).toLocaleString(); } });    prev.current = value;  }, [value]);  return <span ref={ref} className="tabular-nums" />;}
export function LeaderboardList({ entries, highlightId, large }: { entries: LeaderboardEntry[]; highlightId?: string; large?: boolean }) {  return (    <ul className="space-y-2">      <AnimatePresence initial={false}>        {entries.map((e, i) => (          <motion.li key={e.playerId} layout            initial={{ opacity: 0, y: 24 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0 }}            transition={{ type: 'spring', stiffness: 380, damping: 32, delay: i * 0.05 }}            className={`flex items-center gap-3 rounded-2xl px-4 ${large ? 'py-4 text-xl' : 'py-3'}               ${e.playerId === highlightId ? 'bg-brand-500/40 ring-2 ring-brand-500' : 'bg-white/5'}`}>            <span className={`w-9 text-center font-display font-bold ${large ? 'text-2xl' : ''}`}>{MEDAL[e.rank - 1] ?? e.rank}</span>            <span className="flex-1 truncate font-semibold">{e.nickname}</span>            {e.delta > 0 && <span className="text-sm font-semibold text-emerald-400">+{e.delta}</span>}            <span className="font-display font-bold"><Score value={e.score} /></span>          </motion.li>        ))}      </AnimatePresence>    </ul>  );}
