# Load testing

The shared-chat source references k6 load tests but did not contain their implementation. This folder is intentionally left as a placeholder rather than pretending those scripts were present in the supplied source.
